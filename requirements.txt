# Tip-2 Diyabet Chatbot — Python bağımlılıkları
# Kurulum: pip install -r requirements.txt

# Retrieval (mevcut)
numpy>=1.26
torch>=2.1
sentence-transformers>=3.0

# API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
openai>=1.40
httpx<0.28
python-dotenv>=1.0

# Eval (Ragas)
# Chat cevap = Nemotron (src/api/llm.py). Judge = GPT / OpenAI (src/eval/ragas_score.py).
# answer_relevancy yok (embedding ister).
ragas>=0.3

# RAGChecker (bağımsız eval katmanı) — ragas_predict/ragas_score'dan BAĞIMSIZ çalışır.
# BU PAKET ANA ENV'E KURULMAZ: ragchecker -> refchecker -> litellm zinciri
# httpx>=0.28 + transformers<5 ister; bu, yukarıdaki "httpx<0.28" piniyle ve
# retrieval'in transformers 5.x'iyle ÇELİŞİR. Ayrı venv'de kurulur:
#   python -m venv --system-site-packages .venv-ragchecker
#   .venv-ragchecker/Scripts/python.exe -m pip install ragchecker
#   .venv-ragchecker/Scripts/python.exe -m spacy download en_core_web_sm
# Çalıştırma (main ortama DOKUNMAZ):
#   .venv-ragchecker/Scripts/python.exe src/eval/ragchecker_run.py --predictions <path>

# Caching
portalocker>=2.8

# At-rest şifreleme (Faz 5.1 — Fernet)
cryptography>=42.0

# Not: NVIDIA_API_KEY ve OPENAI_API_KEY → frontend/.env (VITE_ öneki olmadan).
